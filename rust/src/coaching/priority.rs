//! Coaching cue priority queue with deduplication.
//!
//! Phase 1b — stub for now.

/// Priority queue for coaching cues.
pub struct PriorityQueue {
    _placeholder: bool,
}

impl PriorityQueue {
    pub fn new() -> Self {
        Self {
            _placeholder: false,
        }
    }
}

impl Default for PriorityQueue {
    fn default() -> Self {
        Self::new()
    }
}

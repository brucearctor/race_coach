# buf.gen.ci.yaml — CI-only template for codegen freshness checks.
# Generates Dart protos only (Rust proto2type requires a local custom plugin).
version: v2
managed:
  enabled: true
plugins:
  - protoc_builtin: dart
    out: lib/generated
